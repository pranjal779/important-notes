terraform workspace -h # -h is help

terraform workspace show

terraform workspace new dev

terraform workspace show

terraform workspace new prd

terraform plan

terraform workspace show

terraform workspace select dev

terraform plan

terraform apply

# terraform .tfstate.d is the folder for terraform workspace command

terraform workspace select default

terraform apply